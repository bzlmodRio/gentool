# load("@rules_bazelrio//:java_rules.bzl", "bazelrio_java_binary")

# bazelrio_java_binary(
java_binary(
    name = "main",
    srcs = ["Main.java"],
    jvm_flags = [
        "-Djava.library.path=.",
    ],
    main_class = "bazelrio_test.Main",
    deps = [
        "@allwpilib//dependencies/java/{{target.parent_folder}}",
    ],
)

