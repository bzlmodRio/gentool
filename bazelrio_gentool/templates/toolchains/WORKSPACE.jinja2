workspace(name = "{{container.repo_name}}")


load("@{{container.repo_name}}//:maven_deps.bzl", "setup_legacy_setup_toolchains_dependencies")

setup_legacy_setup_toolchains_dependencies()

load("@{{container.repo_name}}//toolchains:load_toolchains.bzl", "load_toolchains")

load_toolchains()

register_toolchains(
{%- for config in container.configs %}
    "@local_{{config.short_name_underscore}}//:macos",
    "@local_{{config.short_name_underscore}}//:linux",
    "@local_{{config.short_name_underscore}}//:windows",
{%- endfor %}
)

load("@bazel_tools//tools/build_defs/repo:http.bzl", "http_archive")
http_archive(
    name = "bazel_skylib",
    sha256 = "{{bazel_dependencies.bazel_skylib.sha}}",
    strip_prefix = "bazel-skylib-{{bazel_dependencies.bazel_skylib.version}}",
    url = "https://github.com/bazelbuild/bazel-skylib/archive/refs/tags/{{bazel_dependencies.bazel_skylib.version}}.tar.gz",
)
