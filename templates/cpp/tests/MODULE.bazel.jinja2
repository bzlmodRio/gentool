
bazel_dep(name = "{{target.name.lower()}}", repo_name = "{{target.name.lower()}}", version = "")

local_path_override(
    module_name = "{{target.name.lower()}}",
    path = "..",
)

bazel_dep(name = "rules_bazelrio", version = "0.0.1")
bazel_dep(name = "rules_cc", version = "0.0.2")


{%- if False %}
local_path_override(
    module_name = "rules_bazelrio",
    path = "../../../../rules_bazelrio"
)
{%- for dep in target.dependencies -%}
{%- if "opencv" not in dep.parent_folder %}
local_path_override(
    module_name = "{{dep.name.lower()}}",
    path = "../../../{{dep.parent_folder}}/{{dep.name}}"
)
{%- endif %}
{%- endfor %}
{%- endif %}