--- /dev/null
+++ BUILD.bazel
@@ -0,0 +1,11 @@
+package(default_visibility = ["//visibility:public"])
+
+licenses(["notice"])
+
+cc_library(
+    name = "headers",
+    hdrs = glob([
+        "**",
+    ]),
+    includes = ["include"],
+)
